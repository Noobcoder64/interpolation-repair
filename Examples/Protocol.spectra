spec Protocol

// INPUT VARIABLES

env boolean req;
env boolean cl;

// OUTPUT VARIABLES

sys boolean gr;
sys boolean val;

// ASSUMPTIONS

assumption
    GF ((!req));

// GUARANTEES

guarantee
    G ((cl -> !val));
guarantee
    GF ((gr & val));
