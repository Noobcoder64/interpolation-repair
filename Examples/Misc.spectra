module Protocol

// INPUT VARIABLES

env boolean req;
env boolean cl;

// OUTPUT VARIABLES

sys boolean gr;
sys boolean val;

define
	vall := val;

// ASSUMPTIONS

assumption
    GF(!req);

// GUARANTEES

guarantee
    G(cl -> !vall);
guarantee
    GF(gr and vall);
